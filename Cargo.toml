[package]
name = "antbyte"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "*"
rand = "*"
regex = "*"
clap = { version = "*", features = ["derive"], optional = true }
gif = { version = "*", optional = true }

[features]
default = ["clap", "gif"]
# default = ["clap"]
clap = ["dep:clap"]
gif = ["dep:gif"]
