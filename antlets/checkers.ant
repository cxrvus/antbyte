set size = 16;
set start = top_left;
set colors = bin;
set border = obs;

fn xor = (a, b) => c { c = or(and(-a, b), and(a, -b)); }

ant main {
	C0 = T0;

	D1 = or(M0, OBS);
	D2 = and(D1, M1);

	toggle = and(M0, -OBS);
	M1 = xor(M1, toggle);

	M0 = OBS;

	M2 = or(M2, T7);
	DIE = and(M2, -T7);
}
