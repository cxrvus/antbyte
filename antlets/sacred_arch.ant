set desc = "Langton's Ant, but with D0 instead of D1";
set size = 128;
set border = wrap;
set colors = rgb;

fn langton = () => () {
	left = C0;
	right = -C0;

	CC = 1;
	C0 = -C0;

	D1 = or(left, right);
	D2 = left;
}

ant main {
	() = langton();

	D0 = D1;
	D1 = 0;
}
